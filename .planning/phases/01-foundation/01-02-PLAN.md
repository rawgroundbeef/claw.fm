---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - web/package.json
  - web/tsconfig.json
  - web/tsconfig.node.json
  - web/vite.config.ts
  - web/tailwind.config.js
  - web/index.html
  - web/src/main.tsx
  - web/src/App.tsx
  - web/src/index.css
  - web/public/favicon.svg
autonomous: true

must_haves:
  truths:
    - "Running `pnpm dev` in web/ starts Vite dev server and renders the claw.fm placeholder page"
    - "Placeholder page shows 'claw.fm' heading and 'AI radio, 24/7' tagline styled with Tailwind"
    - "Importing Track type from @claw/shared in web/ compiles without errors"
    - "Tailwind utility classes apply correctly (visible in rendered page)"
  artifacts:
    - path: "web/vite.config.ts"
      provides: "Vite config with React and Tailwind plugins"
      contains: "tailwindcss"
    - path: "web/src/App.tsx"
      provides: "Placeholder landing page component"
      min_lines: 10
    - path: "web/src/main.tsx"
      provides: "React 19 entry point"
      contains: "createRoot"
    - path: "web/index.html"
      provides: "HTML entry with Vite script tag"
      contains: "main.tsx"
  key_links:
    - from: "web/package.json"
      to: "packages/shared"
      via: "workspace:* dependency"
      pattern: '"@claw/shared":\\s*"workspace:\\*"'
    - from: "web/vite.config.ts"
      to: "tailwindcss"
      via: "Vite plugin"
      pattern: "tailwindcss"
    - from: "web/src/main.tsx"
      to: "web/src/App.tsx"
      via: "React root render"
      pattern: "import App"
---

<objective>
Create the React 19 + Vite frontend workspace with Tailwind CSS styling, a clean placeholder landing page showing the claw.fm brand, and verify the shared types package import works from the frontend.

Purpose: Delivers the second half of the deployable skeleton -- a running frontend that matches the visual direction (minimal, monochrome + electric blue) and proves the monorepo cross-package imports work in both directions.
Output: Working React app at localhost:5173 with styled placeholder page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create React 19 + Vite + Tailwind frontend workspace</name>
  <files>
    web/package.json
    web/tsconfig.json
    web/tsconfig.node.json
    web/vite.config.ts
    web/tailwind.config.js
    web/index.html
    web/src/main.tsx
    web/src/App.tsx
    web/src/index.css
    web/public/favicon.svg
  </files>
  <action>
    Create the React + Vite frontend workspace from scratch (do NOT use `npm create vite` -- manual setup avoids React 18 default and unwanted boilerplate):

    1. `web/package.json`:
       - name: "claw-fm-web", private: true
       - dependencies: react (^19), react-dom (^19), @claw/shared (workspace:*)
       - devDependencies: @types/react (^19), @types/react-dom (^19), @vitejs/plugin-react, vite, tailwindcss (^3), @tailwindcss/vite, autoprefixer, typescript
       - scripts:
         - "dev": "vite"
         - "build": "tsc -b && vite build"
         - "preview": "vite preview"

    2. `web/vite.config.ts`:
       ```typescript
       import { defineConfig } from 'vite'
       import react from '@vitejs/plugin-react'
       import tailwindcss from '@tailwindcss/vite'

       export default defineConfig({
         plugins: [react(), tailwindcss()],
       })
       ```

    3. `web/tailwind.config.js`:
       ```javascript
       /** @type {import('tailwindcss').Config} */
       export default {
         content: [
           "./index.html",
           "./src/**/*.{js,ts,jsx,tsx}",
         ],
         theme: {
           extend: {
             colors: {
               'electric': '#0066FF',
             },
             fontFamily: {
               sans: ['Inter', 'SF Pro Display', '-apple-system', 'BlinkMacSystemFont', 'system-ui', 'sans-serif'],
             },
           },
         },
         plugins: [],
       }
       ```

    4. `web/tsconfig.json`:
       - target: ES2022, module: ESNext, moduleResolution: bundler
       - strict: true, skipLibCheck: true
       - jsx: "react-jsx"
       - include: ["src"]

    5. `web/tsconfig.node.json`:
       - For vite.config.ts -- module: ESNext, moduleResolution: bundler
       - include: ["vite.config.ts"]

    6. `web/index.html`:
       ```html
       <!DOCTYPE html>
       <html lang="en">
         <head>
           <meta charset="UTF-8" />
           <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
           <meta name="viewport" content="width=device-width, initial-scale=1.0" />
           <title>claw.fm - AI radio, 24/7</title>
         </head>
         <body>
           <div id="root"></div>
           <script type="module" src="/src/main.tsx"></script>
         </body>
       </html>
       ```

    7. `web/src/main.tsx`:
       ```tsx
       import { StrictMode } from 'react'
       import { createRoot } from 'react-dom/client'
       import App from './App'
       import './index.css'

       createRoot(document.getElementById('root')!).render(
         <StrictMode>
           <App />
         </StrictMode>
       )
       ```

    8. `web/src/index.css`:
       ```css
       @tailwind base;
       @tailwind components;
       @tailwind utilities;
       ```

    9. `web/src/App.tsx` -- placeholder landing page matching CONTEXT.md visual direction:
       - Clean white background, centered content
       - "claw.fm" as large bold heading in black
       - "AI radio, 24/7" tagline in gray
       - Electric blue (#0066FF) horizontal accent line below tagline
       - Minimal, static, no animations (per CONTEXT.md: "Static page -- no background animation or motion")
       - Import Track type from @claw/shared somewhere (e.g., a type annotation or a comment) to verify cross-package import compiles

       Example structure:
       ```tsx
       import type { Track } from '@claw/shared'

       export default function App() {
         return (
           <div className="min-h-screen bg-white flex items-center justify-center font-sans">
             <div className="text-center">
               <h1 className="text-7xl font-bold tracking-tight text-black">
                 claw.fm
               </h1>
               <p className="mt-4 text-xl text-gray-500">
                 AI radio, 24/7
               </p>
               <div className="mt-8 w-16 h-1 bg-electric mx-auto" />
             </div>
           </div>
         )
       }
       ```

    10. `web/public/favicon.svg` -- minimal SVG favicon:
        - Simple radio wave or music note icon in electric blue
        - Keep it tiny (under 1KB)

    11. Run `pnpm install` at project root to install web workspace dependencies.

    Do NOT install lucide-react, motion, or zustand yet -- those are needed in Phase 4+, not Phase 1.
    Do NOT use TypeScript path aliases. Use @claw/shared via workspace protocol.
    Do NOT use Tailwind v4 -- use v3 (stable, better documented per RESEARCH.md).
  </action>
  <verify>
    - `cd web && pnpm dev` starts Vite dev server (default port 5173)
    - Opening http://localhost:5173 in browser shows the claw.fm placeholder page
    - `cd web && pnpm exec tsc --noEmit` passes (confirms @claw/shared import works)
    - Tailwind classes render correctly (inspect element shows utility classes applied)
  </verify>
  <done>
    React 19 + Vite frontend runs locally. Placeholder page displays "claw.fm" heading and "AI radio, 24/7" tagline with Tailwind styling (white bg, black text, electric blue accent). @claw/shared import compiles without errors. Build succeeds with `pnpm build`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add root dev scripts and verify full monorepo integration</name>
  <files>
    package.json
  </files>
  <action>
    Update root package.json to add convenience scripts for running both workspaces:

    1. Add scripts to root `package.json`:
       ```json
       {
         "scripts": {
           "dev:api": "pnpm --filter claw-fm-api dev",
           "dev:web": "pnpm --filter claw-fm-web dev",
           "build:web": "pnpm --filter claw-fm-web build",
           "typecheck": "pnpm -r exec tsc --noEmit"
         }
       }
       ```

    2. Run full integration verification:
       - `pnpm typecheck` -- all three packages (shared, api, web) pass TypeScript checks
       - `pnpm dev:api` -- API starts and /health responds
       - `pnpm dev:web` -- frontend starts and renders placeholder

    3. Verify @claw/shared is correctly linked in both consumers:
       - `pnpm --filter claw-fm-api exec -- node -e "require('@claw/shared')"` or equivalent TypeScript check
       - `pnpm --filter claw-fm-web exec -- node -e "require('@claw/shared')"` or equivalent TypeScript check

    This task is small but critical -- it proves the monorepo works as a unified system, not just individual workspaces.
  </action>
  <verify>
    - `pnpm typecheck` passes for all workspaces (0 errors)
    - `pnpm dev:api` starts API on port 8787
    - `pnpm dev:web` starts frontend on port 5173
    - Both api and web can import from @claw/shared without errors
  </verify>
  <done>
    Root scripts work for running API and frontend. TypeScript compilation passes across all workspaces. Shared types package is importable from both API and frontend. Monorepo is fully integrated.
  </done>
</task>

</tasks>

<verification>
1. `pnpm dev:web` starts Vite dev server on port 5173
2. Browser shows claw.fm placeholder with correct styling (white bg, black heading, gray tagline, blue accent)
3. `pnpm typecheck` passes for all workspaces (shared, api, web)
4. `pnpm dev:api` still works (regression check)
5. @claw/shared Track type is importable in web/src/App.tsx
</verification>

<success_criteria>
- React 19 + Vite frontend renders placeholder page at localhost:5173
- Tailwind CSS applies correctly (electric blue accent, system fonts, centered layout)
- Placeholder shows "claw.fm" heading and "AI radio, 24/7" tagline per CONTEXT.md
- @claw/shared imports work from both api and web workspaces
- Root convenience scripts (dev:api, dev:web, typecheck) work
- Full monorepo integration verified end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
