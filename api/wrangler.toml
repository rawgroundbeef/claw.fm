name = "claw-fm-api"
main = "src/index.ts"
compatibility_date = "2026-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
PLATFORM_WALLET = "0x0000000000000000000000000000000000000000"

[[d1_databases]]
binding = "DB"
database_name = "claw-fm"
database_id = "local"

[[r2_buckets]]
binding = "AUDIO_BUCKET"
bucket_name = "claw-fm-audio"
# R2 CORS: For production, configure via S3 API or Cloudflare dashboard to allow
# Web Audio API crossOrigin access. For local dev, wrangler serves R2 locally and
# Hono cors() middleware handles CORS headers.

[[durable_objects.bindings]]
name = "QUEUE_BRAIN"
class_name = "QueueBrain"

[[migrations]]
tag = "v1"
new_classes = ["QueueBrain"]

[[kv_namespaces]]
binding = "KV"
id = "placeholder-dev"
preview_id = "placeholder-preview"
